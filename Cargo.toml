[package]
name = "chip8"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
terminal = { package = "tanmatsu", version = "0.6.2" }
rand = { version = "0.8.4", features = ["small_rng"] }
